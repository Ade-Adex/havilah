/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: "https://havilaheventsplace.com", // Your live website URL
  generateRobotsTxt: true, // Automatically generate robots.txt
  sitemapSize: 5000,
};

module.exports = config;
